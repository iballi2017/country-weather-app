import { MathFixedPipe } from './math-fixed.pipe';

describe('MathFixedPipe', () => {
  it('create an instance', () => {
    const pipe = new MathFixedPipe();
    expect(pipe).toBeTruthy();
  });
});
